Doporučený postup:
1. python block7c_entry.py
2. python block8a_entry.py
3. python block9a_entry.py

Volitelně:
- vytvoř `config/live_broker.yml`
- nastav `BROKER_API_KEY`
- nastav `BROKER_API_SECRET`
- nastav `BROKER_BASE_URL`

Výstupy:
- `.state/block9a_live_broker_probe.json`
- `block9a_output.json`

Poznámka:
je to bezpečný live scaffold, ne ostré odesílání orderů.
Bez explicitního odemčení zůstává session guard blokující.
