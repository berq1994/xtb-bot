name: XTB Bot Alerts

on:
  schedule:
    # běh každých 15 minut
    - cron: "*/15 * * * *"

  # umožní ruční spuštění tlačítkem
  workflow_dispatch:

jobs:
  run-bot:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install yfinance requests matplotlib

      - name: Run bot
        env:
          TELEGRAMTOKEN: ${{ secrets.TELEGRAMTOKEN }}
          CHATID: ${{ secrets.CHATID }}
          FMPAPIKEY: ${{ secrets.FMPAPIKEY }}
          GMAILPASSWORD: ${{ secrets.GMAILPASSWORD }}

          TZ: Europe/Prague
          ALERTS_START: "15:30"
          ALERTS_END: "21:00"
          ALERT_THRESHOLD_PCT: "5"
          REPORT_TIME: "15:30"

          EMAIL_ENABLED: "true"
          EMAIL_SENDER: "berq1994@gmail.com"
          EMAIL_RECEIVER: "berq1994@gmail.com"

        run: |
          python ahoj.py
