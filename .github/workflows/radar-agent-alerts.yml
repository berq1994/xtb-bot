name: radar-earnings

on:
  workflow_dispatch:
  schedule:
    - cron: "0 7 * * 1"  # Pondělí 07:00 UTC (08:00 Prague)

concurrency:
  group: radar-earnings
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; else pip install yfinance feedparser requests PyYAML; fi

      - name: Restore state cache
        uses: actions/cache@v4
        with:
          path: .state
          key: radar-state-${{ github.ref_name }}-${{ github.run_number }}
          restore-keys: |
            radar-state-${{ github.ref_name }}-

      - name: Run earnings
        env:
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
        run: |
          python run_agent.py earnings

      - name: Save state cache
        uses: actions/cache@v4
        with:
          path: .state
          key: radar-state-${{ github.ref_name }}-${{ github.run_number }}