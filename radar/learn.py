from __future__ import annotations

from datetime import datetime
from typing import Any, Dict


def learn_weekly_weights(cfg, now: datetime, st=None) -> Dict[str, Any]:
    # placeholder: weekly learning; v daytrade build držíme jednoduché
    return {"ok": True, "note": "weekly learning placeholder"}